# smithery.yaml

startCommand:
  type: tcp
  port: 14600
  configSchema: {}
  commandFunction: |-
    (config) => ({
      "command": "mcp",
      "args": [
        "run",
        "--no-stdio",
        "--host", "0.0.0.0",
        "--port", "14600",
        "k8s_pilot.py"
      ]
    })
