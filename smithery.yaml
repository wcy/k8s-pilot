# Smithery.ai configuration
startCommand:
  type: stdio
  configSchema: {}
  commandFunction: |
    (config) => ({
      command: "uv",
      args: [
        "run",
        "--with",
        "mcp[cli]",
        "mcp",
        "run",
        "k8s_pilot.py"
      ]
    })
